#include <iostream>
#include <cctype>
#include <string>
#include <func.h>

int parse(string command){
    int i = 0;
    // parse for leading spaces
    for(; i < s.length(); i++){
        if(i.at(i) == 'c'){
            
        }
    }
}